// Operador de Strings (String operator)

//Comparison (Comparacao)
console.log( 'a' == 'a')

//Concatenation (Concatenação)
//Retorna a união de duas strings
let alpha = 'alpha'
console.log( alpha + 'bet')
console.log( alpha += 'bet')